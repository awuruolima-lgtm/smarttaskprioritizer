# Database file path (SQLite for local development)
DATABASE_URL=sqlite:///tasks.db

# FastAPI server port
PORT=8000

# JWT secret key for authentication (if you plan to add auth later)
JWT_SECRET=your_secret_key_here
